create table persons(id bigint auto_increment, person_name varchar(30), zip varchar(10), age bigint, email varchar(30),
    taj varchar(30), number_of_vaccination bigint, last_vaccination_date timestamp, primary key (id));